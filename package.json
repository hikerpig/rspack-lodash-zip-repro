{
  "name": "rspack-lodash-zip-repro",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "cross-env NODE_ENV=development rspack serve",
    "build": "cross-env NODE_ENV=production rspack build"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "lodash.zip": "^4.2.0",
    "@rspack/cli": "^1.0.3",
    "@rspack/core": "^1.0.3"
  }
}
